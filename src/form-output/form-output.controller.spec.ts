import { Test, TestingModule } from '@nestjs/testing';
import { FormOutputController } from './form-output.controller';

/*describe('FormOutput Controller', () => {
  let module: TestingModule;
  beforeAll(async () => {
    module = await Test.createTestingModule({
      controllers: [FormOutputController],
    }).compile();
  });
  it('should be defined', () => {
    const controller: FormOutputController = module.get<FormOutputController>(
      FormOutputController,
    );
    expect(controller).toBeDefined();
  });
});*/
describe('run', () => {
  it('should exists', () => {
    expect(true).toBeTruthy();
  });
});
